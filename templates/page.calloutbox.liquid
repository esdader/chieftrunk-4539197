<article class="callout-article">
    <header class="callout-header">
        <h1 class="callout-title">{{ page.title }}</h1>
    </header>
    <div class="callout-body">
        {{ page.content }}
    </div>
    <footer class="callout-footer">
    </footer>
</article>